{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Python: Run NCI",
            "type": "python",
            "request": "launch",
            "program": "${workspaceFolder}/run_nci.py",
            "console": "integratedTerminal",
            "justMyCode": true,
            "cwd": "${workspaceFolder}"
        }
    ]
}